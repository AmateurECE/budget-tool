[package]
name = "budget-models"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { version = "1.4.4", features = ["postgres"] }
dotenv = "0.15.0"
clap = { version = "3.1.8", features = ["derive"] }
